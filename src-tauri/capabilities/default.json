{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability for the tax calculator.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
